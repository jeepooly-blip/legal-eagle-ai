[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn legal_eagle.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
